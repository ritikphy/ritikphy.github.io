# Architecture

This project separates:

- structure (`index.html`)
- appearance (`css/`)
- behaviour (`js/`)
- content (`data/`)
- media (`images/`, `pdf/`)

Keep content changes in `data/` whenever possible.
